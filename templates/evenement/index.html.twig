{% extends 'base.html.twig' %}

{% block title %}Liste des événements{% endblock %}

{% block body %}
<h1>Liste des événements</h1>

{% for evenements in evenements %}
    <p><a href=" {{ path('show_evenement', {'id': evenement.id}) }} ">{{ evenement }}</a></p>
{% endfor %}

{% endblock %}
